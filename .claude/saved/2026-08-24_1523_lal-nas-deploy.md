# Handover — LAL (Listen And Learn), volgende ronde = NAS-deploy bouwen (2026-08-24 15:23)

## Waar we mee bezig zijn
Generieke taal-leer-app (nu Spaans→NL), boek zin-voor-zin met progressieve hulp
(luister → ondertiteld/hovers → NL-zin → Gemini-uitleg), hoofdstukken, stemkeuze.
Draait lokaal via Vite dev-server. Repo staat public op GitHub.

## Onderhanden werk (NIET gecommit — werktree-wijzigingen)
- **Interface-opschoning zojuist gebouwd** maar nog niet gecommit: `src/App.tsx`,
  `src/index.css` (⚙-knop met inklapbaar paneel voor stem/snelheid/boek/hoofdstuk;
  Cloud-stemlijst ingekort tot LatAm Neural2 + paar Chirp3-HD via `curateCloudVoices`).
- **`docs/DEPLOY.md`** (untracked): volledig plan voor NAS-deploy — AKKOORD van user.
- **`docs/STATUS.md`** gewijzigd (veel TODO's toegevoegd deze sessie).
- → Eerst even committen aan het begin van de volgende ronde.

## Volgende stap (afgesproken): DEPLOY.md bouwen
Kleine **Node-backend** (server/) die: keys server-side uit env houdt, `/api/translate`,
`/api/explain`, `/api/tts`, `/api/voices` aanbiedt, en resultaten **cachet op een gemount
NAS-volume** (gedeeld tussen 2 gebruikers: user + vrouw, eigen apparaten/eigen voortgang).
Client omzetten naar `/api/*` (keys + browser-caches eruit). Dockerfile + compose +
Container Manager. Alles staat in **`docs/DEPLOY.md`**.

## Openstaande beslissingen (vóór/bij bouwen bevestigen — staan onderaan DEPLOY.md)
- Poort (voorstel 8080?).
- NAS-volumepad (voorstel `/volume1/docker/lal/cache`).
- Cache-vorm: losse bestanden (default) of SQLite voor tekstresultaten.

## Relevante context / valkuilen
- Git: branch `master`, laatste commit `97ae590`, gepusht naar
  `github.com/ton-bosma/lal-listenandlearn` (public). **Push-valkuil:** globale git-config
  forceert `credential.username=email` + Windows credential manager → git push vraagt om
  wachtwoord en faalt. Werkende workaround (gh is ingelogd):
  `b64=$(printf 'x-access-token:%s' "$(gh auth token)" | base64 -w0) && git -c credential.helper= -c credential.username= -c http."https://github.com/".extraheader="AUTHORIZATION: basic $b64" push origin master`
- **Secrets**: `.env` (VITE_GOOGLE_TRANSLATE_KEY = Cloud-key voor Translate+TTS,
  VITE_GEMINI_KEY) en `apikey.txt` staan in `.gitignore` — nooit committen.
- **Gemini**: model-alias `gemini-flash-latest`; user heeft €25 prepay gedaan (Optie B);
  `maxOutputTokens:2048` (thinking-tokens kapten anders af).
- **npm** op deze machine: prefix `HOME="$USERPROFILE"`.
- **Dev-server** draait als achtergrondtaak op http://localhost:5174 (kan verlopen zijn).
- **EPUB** gebouwd (`src/lib/epub.ts`, jszip) maar front-end bewust terug op PDF
  (`accept="application/pdf"`) — "ging nog niet lekker"; geparkeerd.
- **Auteursrecht**: OceanofPDF-bestanden in Downloads = illegaal, niet verwerken. App leest
  alleen legaal verkregen boeken (user's Manolito-PDF in Downloads voor testen).

## Documentatie (leidend — niet overtypen)
- `docs/SPEC.md` (functioneel + keuzes), `docs/STATUS.md` (fasestand + groot TODO-blok met
  o.a. front-matter overslaan, woorden-markeren/overhoren, uitspraak-check, vocabulaire
  vooraf, vervolgvraag-op-uitleg, sneller-lezen-features, taal-als-instelling),
  `docs/DEPLOY.md` (deploy-plan), `README.md`.
